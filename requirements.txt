# Core ML
torch>=2.0.0
torchvision>=0.15.0
timm>=1.0.0
numpy>=1.24.0
Pillow>=9.0.0
scipy>=1.10.0

# Training utilities
tqdm>=4.65.0
tensorboard>=2.14.0
scikit-learn>=1.3.0

# Dataset (pinned <4.0: NYUv2 HF dataset uses a loading script)
datasets>=2.14.0,<4.0
h5py>=3.9.0

# Export
onnx>=1.14.0
onnxscript>=0.6.0

# Visualization
matplotlib>=3.7.0

# ── Teacher inference (HPC only, not needed for student training) ──────
# DA3: install from source -- git clone https://github.com/ByteDance-Seed/Depth-Anything-3 && pip install -e Depth-Anything-3/
# ultralytics                # YOLOv8 for YOLO-seeded SAM2 prompting
# sam2: pip install git+https://github.com/facebookresearch/sam2.git
